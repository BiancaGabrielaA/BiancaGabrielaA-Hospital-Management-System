#ifndef APPOINTMENT_HPP
#define APPOINTMENT_HPP

#include <string>

class Appointment {
    std::string id;
    std::string patientId;
    std::string doctorId;
    std::string date;
    std::string status;
};


#endif
